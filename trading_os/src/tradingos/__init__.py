"""tradingos — India equity (NSE) backtesting, paper and live trading platform.

Note: the architecture spec called this package ``platform/``; that name shadows
the Python stdlib ``platform`` module, so the package is ``tradingos`` while the
CLI entry point remains ``platform``.
"""

__version__ = "0.1.0"
